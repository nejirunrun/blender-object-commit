schema_version = "1.0.0"

id = "object_commit"
version = "0.1.0"
name = "Object Commit"
tagline = "Per-object git-like commits stored inside the .blend"
maintainer = "neji"
type = "add-on"
blender_version_min = "5.0.0"
license = ["SPDX:GPL-3.0-or-later"]
tags = ["Object", "Mesh"]
